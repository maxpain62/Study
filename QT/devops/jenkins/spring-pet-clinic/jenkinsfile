pipeline {
    agent 'any'
    stages {
        stage ('vcs') {
            steps {
                git branch: 'main', url: 'https://github.com/gauravlab01-outlook/spring-petclinic.git'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn package'
            }
        }
    }
}